import { NgWebsitePage } from './app.po';

describe('ng-website App', () => {
  let page: NgWebsitePage;

  beforeEach(() => {
    page = new NgWebsitePage();
  });

  /*
  it('should see the header', () => {
    page.navigateTo();
    expect(page.getParagraphText()).toEqual('Our Planets - Using Angular 2 Material');
  });
  */
});
