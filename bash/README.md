Problem
=================

Which of the following command lines can be used to convert a file containing DOS-style CR-LF line endings into Unix-style LF line endings? 

Assume for this question that the DOS-style file is called 'dosfile', and we want the modified contents in 'unixfile'

Solution
-----------------

The answer get a partial solution. If you have a file with "name surname", this sorts only surnames:


